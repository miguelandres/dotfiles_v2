defaults:
  create:
    force: true
  exec:
    echo: true
  brew:
    force_casks: true
  link:
    force: true
    relink: true

steps:
  - subconfig:
      - zsh/zsh.yaml
      - zsh/zsh-mac.yaml
      - zsh/docker.yaml
      - zsh/zsh-mac-cloud.yaml
      - git/personal.yaml
      - vim/vim.yaml
      - tmux/tmux.yaml
      - mac/basics.yaml
      - mac/personal.yaml
      - vim/vim.yaml
