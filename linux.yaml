defaults:
  create:
    force: true
  exec:
    echo: true
  brew:
    force_casks: true
  link:
    force: true
    relink: true

steps:
  - subconfig:
      - base.yaml
      - zsh/base.yaml
      - zsh/linux.yaml
      - git/personal.yaml
      - tmux/tmux.yaml
      - vim/vim.yaml
